include ../../Ui-kit/icon/icon

mixin social-box({iconsName, className})
    div(class= `${className} social-box`)
        each name in iconsName
            div(class= `${className}-icon-container social-box__icon-container`)
                +icon(name, "social-box__icon")
