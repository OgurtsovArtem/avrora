mixin filter-button({text, className, active})
    button(class=className ? `${className} ${active ? "filter-button filter-button_active" : 'filter-button'}`: `${active ? "filter-button filter-button_active" : 'filter-button'}`)=text